# CONTEXT — 開発の前提と経緯

READMEが「どう構築するか」なら、これは **「なぜそうなっているか」と「今どこにいるか」**。
新しく参加する開発者（人・AI問わず）は先にこれを読むこと。

---

## 1. システム全体像

名古屋テレビ放送 技術局デジタルマーケティングセンターで運用している
「視聴率分析ダッシュボード」の監視レイヤー。

ダッシュボード本体は複数の入力を1画面に統合している。

```
iNex（ビデオリサーチ）  ──> 視聴率データ（毎分推移）  ┐
Gガイド番組表           ──> EPGデータ                 ├──> ダッシュボード
Open-Meteo             ──> 天気データ                 │    (AWS Amplify)
5局オンエア収録システム ──> 動画 ──> AI動画解析 ──> 番組メタデータ ┘
```

各システムは別々のGitHubリポジトリで開発されている
（「視聴率ダッシュボード」「動画収録システム」「動画AI分析システム」など）。

**このリポジトリはそれらのどれでもない。** 3系統をまたいで
「期待したデータが期待した時刻に来ているか」だけを見る独立した監視。

### なぜ独立させたか

各リポジトリに監視ロジックを分散させると全体像が誰にも見えなくなる。
各システムは成果物をS3に置くだけ、判断と通知はこちらに集約する、という役割分担。

### 監視の基本方針

**「異常を検知する」ではなく「期待した正常が来ないことを検知する」。**

障害の大半はエラーログを出さず沈黙する形で現れる。収録PCが固まった、
Lambdaが起動しなかった、といったケースは例外すら発生しない。
放送局には番組表という完璧な期待値マスタがあるので、それを基準に実績と突合する。

---

## 2. 実データの構造

### 2-1. EPG（Gガイド番組表）

```
s3://bangumi-info/epg-all/bangumi_{YYYYMMDD}.json
```

2026-06-15より前はCSV形式。**監視は将来分のみ対象なのでJSONだけ扱う。**

```json
{
  "broadcast_date": "20260831",
  "ggm_group_id": "73",
  "generated_at": "2026-08-31T13:00:03.928938+09:00",
  "program_count": 386,
  "programs": [
    {
      "ChannelId": "0x0C08",
      "ChannelName": "NHKEテレ",
      "Pid": "781306",
      "SeId": "0x0C08-6700",
      "StartTime": "202608310530",
      "EndTime": "202608310550",
      "ProgramTitle": "しあわせ気分のイタリア語（21）古い水車小屋を訪れる",
      "ProgramDetail": "使うとしあわせな気分になれる..."
    }
  ]
}
```

実データ（2026-08-31）で確認した性質:

| 項目 | 実測 | 含意 |
|---|---|---|
| 収録局数 | 9局（在名民放5 + NHK2 + 三重 + ぎふ） | 監視対象3局だけ抽出する |
| 番組数 | 386件（局あたり23〜72件） | 編成次第で大きく変動する |
| `Pid` | 386件中 **68件が `"-1"`** | **一意キーに使えない**（テレビショッピング等） |
| `SeId` | `0x0C20-5345` 形式 | 一意キーが要るならこちら |
| `(ChannelId, StartTime)` | 重複ゼロ | 実際の突合キーはこれ |
| 時刻形式 | `YYYYMMDDHHMM`（JST、秒なし） | |

**1日の範囲は 04:30頃 〜 翌朝05:20頃。**
`bangumi_20260831.json` には 9/1 04:55開始の番組まで入っている。
つまり深夜番組は前日ファイルに載る。**終了時刻がある日に属する番組を
漏れなく拾うには、複数日のファイルを読んでマージする必要がある。**

**時間軸のズレは常時存在する。** 8/31の実データでは
東海テレビに1分、テレビ愛知に5分、三重テレビに60分のギャップがあった。
フィラー番組や編成の都合によるもので異常ではない。

**1日7回上書きされる。** Lambdaのcronは 04/10/13/15/18/21/23時。
`generated_at` はその最終実行時刻になる。

### 2-2. 動画

```
s3://bangumi-info/movie/{ch}/{番組終了日}/{CH}_{番組開始日}_{HHMM}00.mp4
```

**フォルダ日付＝終了日、ファイル名＝開始日**。日跨ぎ番組だけズレる。

```
08/31 23:09-23:15 → movie/ch6/20260830/CH6_20260830_230900.mp4  (前日扱い)
08/31 23:15-00:15 → movie/ch6/20260831/CH6_20260830_231500.mp4  ← 日跨ぎ
09/01 00:40-01:15 → movie/ch6/20260831/CH6_20260831_004000.mp4
```

仕様（`ffprobe` 実測、CH6_20260827_060000.mp4）:

```
codec_name = h264
width      = 1280
height     = 720
bit_rate   = 802918        (映像のみ)
ファイルサイズ 810 MB / 7194秒 = 118,000 バイト/秒（総 約944kbps）
```

ほぼ固定ビットレートなので**サイズが尺に比例する**。
この性質を使って、ffmpegなしでサイズ妥当性を検査している。

収録は**全日24時間**。EPGの全番組が対象。

### 2-3. AI分析結果

```
s3://bangumi-info/results/{ch}/{番組終了日}/{CH}_{番組開始日}_{HHMM}00_corners.csv
```

Gemini で動画を解析し、コーナー単位に区切ったもの。BOM付きUTF-8。

```
broadcast_date,channel,filename,start_sec,end_sec,title,summary,tags,segment
```

実データ（`CH6_20260828_060000_corners.csv`、ドデスカ！2時間）:

| 項目 | 実測 |
|---|---|
| 行数 | 62 |
| `start_sec` 先頭 | 0.000 |
| `end_sec` 末尾 | 7194.160（119.9分） |
| 区間の隙間・重複 | **ゼロ**（完全に連続） |
| `segment` の語彙 | news 17 / cm 10 / feature 10 / weather 7 / other 7 / ent 4 / sports 3 / opening 2 / live 1 / sponsor 1 |
| CM比率 | 約18% |

`filename` 列には対応する動画のファイル名が入る。これがパスと一致するかで
**別番組の結果を書き込む事故**を検知できる。

**分析対象は全番組ではない。** 05:30〜08:30 または 15:30〜19:00 に
一部でも重なる番組のみ。8/31のメ～テレなら6件（04:55 / 05:50 / 06:00 /
08:00 / 14:42 / 15:40）。

### 2-4. チャンネル対応（名古屋のリモコンキーID）

| ch | ChannelId | 局 | 収録 |
|---|---|---|---|
| ch1 | 0x0C10 | 東海テレビ | ○ |
| ch4 | 0x0C28 | 中京テレビ | ○ |
| ch5 | 0x0C18 | CBC | 未 |
| ch6 | 0x0C20 | メ～テレ | ○ |
| ch10 | 0x8430 | テレビ愛知 | 未 |

将来5局化する。環境変数 `CHANNELS` に足すだけでコード修正は不要。

---

## 3. 設計判断とその理由

**期待キューを保存しない**

DBに保存せず、Lambda起動のたびにEPGを読み直して生成する。
EPGが1日7回更新されるため、朝にスナップショットを取ると、
高校野球の延長やニュース特番で編成が変わったときに
「ありもしない番組が未達」「変更後の番組を監視していない」が両方起きる。
毎回作り直せば自動追従する。

**DynamoDBは通知の重複抑止だけに使う**

判定はステートレスに計算する。DBが読めなくても監視は止まらない
（例外を握り潰す実装）。15分ごとに走るため、抑止がないと
同じ障害で1日96通届く。TTL 14日で自動削除。

**EPGの時間軸ズレはP1にしない**

実データに1〜5分のズレが常時ある（§2-1）。厳密に見ると毎日鳴って
すぐ誰も読まなくなる。5分以下は無視、検出しても日次サマリのみ。
**ファイルそのものが無い場合だけP1。** 期待キューが空になると
「未達ゼロ＝全部正常」に見えてしまうため。

**通知は局単位でロールアップ**

収録PCが落ちると数十番組が一斉に未達になる。番組ごとに送ると
1回の障害で数十通飛ぶ。未達の局数で重要度を切り替える
（2局以上=P1で収録PC/回線を疑う、1局=P2でチューナーを疑う）。

**異常ゼロの日も日次サマリを送る**

届かない日は監視Lambda自体が止まっているということ。監視の監視。

**AI分析は成功/失敗の二値で見ない**

Geminiは失敗せずにそれっぽい嘘を返す。CSVの内容妥当性検査
（区間の連続性、カバレッジ、同一タイトルの連続）が実質的な生成AI監視になる。

**存在確認はLISTで一括取得**

番組ごとの `head_object` ではなく該当日フォルダを `list_objects_v2` で
まとめて読む。1実行あたりLIST 6回程度。サイズも同時に取れるので
③のサイズ検査が無料でできる。

**SNSのSubjectは英数字のみ**

ASCII・100文字未満という制約があるため。日本語は本文に入れる。

---

## 4. 検討して却下した案

**再提案しないこと。** 議論の上で見送りを決めている。

| 案 | 却下の理由 |
|---|---|
| 収録PCのハートビート監視 | 「収録→S3アップ」がセットなので、S3不在検知で十分。番組は連続しているのでPCが死ねば次の番組の締切で必ず引っかかる。原因切り分けは現物のログを見る |
| 分析結果の1次判定（動画アップ+2h） | 視聴率が翌日にならないと出ないため、分析も翌朝までに揃えばよい。翌朝9:00の1回のみ |
| ECS / Step Functions | 810MBならLambdaの `/tmp`（10GB）に収まる。単一Lambdaで足りる |
| Range GET / faststart対応 | 同上。ファイルが小さいので部分取得の必要がない |
| SESで `@nagoyatv.com` から送信 | DNS（SPF/DKIM）の変更が要り情シス依頼で待ちが発生する。当面SNSで進める。送信部は `aws_util.publish()` に切り出してあるので差し替えは数行 |
| Terraform | AWS操作はマネジメントコンソール（GUI）で行う運用。手順はREADMEにGUI前提で記述 |
| Teams Webhook通知 | メールで開始。将来の選択肢として保留 |

---

## 5. 現在地

**⚠️ 実物のS3に対してはまだ一度も実行していない。**

| 項目 | 状態 |
|---|---|
| ①EPG・②動画到達・③サイズ・④分析到達・⑤分析内容 | 実装済み |
| 画音品質（blackdetect等） | 未実装 |
| ローカル検証 | EPG 1本・CSV 1本のサンプルでのみ確認 |
| AWSリソース | **未作成**（IAM/Lambda/DynamoDB/SNS/Scheduler いずれも） |
| 実データでの突合 | 未実施 |

検証済みの内容:

- EPGから3局85番組を抽出し、S3キーを生成（日跨ぎ含めて正しい形）
- 時間軸チェックが1分ギャップを誤検知しないこと
- 分析対象判定が8/27の実ファイル6本と件数一致
- CSV検査が正常データを正常と判定し、途中切れ・別番組混入を検出

**次のステップは README のフェーズ1**。IAMロールとLambdaだけ作って
`{"mode": "watch"}` を空撃ちし、CloudWatch Logs の出力を見る。
ここで初めて実物のS3と突合が走る。

**想定外の未達が大量に出たらキー規則の理解がどこかズレているサイン。**
ログに出る `s3://...` を実際のS3コンソールと1件突き合わせれば分かる。

---

## 6. 未確定・要確認

- **`VIDEO_SIZE_TOLERANCE`（±35%）は実測1本から引いた暫定値。**
  数日運用してサイズ分布を見てから絞る。当面は日次サマリのみでP3扱い
- **`ANALYSIS_WINDOWS` は分析システム側の実装と二重管理になっている。**
  当初 15:30-19:30 と想定したが実際は 15:30-19:00 だった。
  将来的には分析システム側の設定を参照する形にしたい
- 視聴率データ（iNex）と天気データ（Open-Meteo）は監視対象外。
  ダッシュボードの入力としては同格なので、`results/` と同じ要領で追加できる
- 収録PCのディスク空き容量。収録は生放送で失敗すると取り返しがつかず、
  停止原因として最も多いのがディスク満杯。唯一「事前に分かる」故障だが
  S3不在検知では拾えない。収録PC側のタスクスケジューラで日次1回、
  空き容量をメールするだけのスクリプトを別途用意するのが有効

---

## 7. 開発時の注意

- `csv_check.py` はAWS非依存。CSVを渡せば単体でテストできる
- `config.py` は設定値のみでロジックを持たない。閾値の調整はここだけで完結する
- `tests/fixtures/` に実データのサンプルがある。`python tests/local_test.py` で
  EPG解析とCSV検査を、`python tests/dryrun.py` でhandlerの通し実行を確認できる
  （どちらもAWSに接続しない）
- Lambdaのハンドラは `handler.lambda_handler`。
  既定の `lambda_function.lambda_handler` から変更が必要
- zipは `src/` の中身が直下に来るように作る
- S3への書き込み権限は付けない。監視は読み取りのみ
